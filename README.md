# AI-Document-Processing-System
FastAPI based AI document processing API that extracts invoice fields from PDFs using PDF text extraction, validates the result with Pydantic, and returns structured JSON.
